;; Package list

;; Because we want to
(defvar required-packages
  '( magit
     yasnippet
     pretty-lambdada
     paredit
     auto-complete
     cperl-mode 
     smex
     color-theme
     color-theme-solarized
     color-theme-buffer-local
     browse-kill-ring     
     smooth-scrolling
     confluence
     jira
     git-commit-mode
     framemove
     org-plus-contrib
     )
  "a list of packages to ensure are installed at launch.")

(provide 'mjr-package-list)
